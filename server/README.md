# Server boundary

Server-only orchestration, request handling, and infrastructure composition belong here.
Credential readers and external service clients must remain server-side and must not be imported by Client Components.
